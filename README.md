# Testing cloudbuild

This is a test of cloudbuild. The `cloudbuild.yaml` file has been updated to run Go tests and execute the main Go program. The `go.mod` file has also been updated to specify the module name and Go version.
